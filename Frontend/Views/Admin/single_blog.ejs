<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/Style/style.css">
    <title>Document</title>
</head>

<body>
    <div class="blog">
        <h4>
            <%= blog.title %>
        </h4>
        <p><strong>Category:</strong>
            <%= blog.category %>
        </p>
        <div class="blog-content">
            <p>
                <%- blog.content %>
            </p>
        </div>
        <div class="small-container"
            style="display: flex; justify-content: space-evenly; align-items: flex-start; flex-wrap: wrap; gap: 20px; padding: 20px; background-color: #f9f9f9; border-radius: 10px;    width: 1200px;">
            <div class="image-container"
                style="width: 550px; height: 400px; text-align: center; background: white; padding: 15px; border-radius: 10px; box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);">
                <h2 style="margin-bottom: 10px; font-size: 18px; color: #333;">Blog Picture</h2>
                <% if (blog.image.url) { %>
                    <img src="<%= blog.image.url %>" alt="Blog Image"
                        style="display: block; object-fit: contain; height: auto; width: auto; border-radius: 8px;">
                    <% } %>
            </div>

            <div class="details-container"
                style="flex: 1; width: 550px; height: 400px; background: white; padding: 15px; border-radius: 10px; box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);">
                <div class="tags" style="margin-bottom: 15px;">
                    <% if (Array.isArray(blog.tags) && blog.tags.length> 0) { %>
                        <% for (let tag of blog.tags) { %>
                            <span
                                style="background: #007bff; color: white; padding: 5px 10px; border-radius: 15px; font-size: 14px; margin-right: 5px; display: inline-block;">
                                #<%= tag.trim() %>
                            </span>
                            <% } %>
                                <% } else { %>
                                    <p style="color: #666; font-size: 14px;">No tags available</p>
                                    <% } %>
                </div>

                <div class="date">
                    <div class="create_date" style="margin-bottom: 10px;">
                        <h4 style="color: #444; font-size: 16px; margin-bottom: 5px;">Created At</h4>
                        <p style="color: #555; font-size: 14px;">
                            Time: <%= new Date(blog.created_date).toLocaleString('en-US', { hour: 'numeric' ,
                                minute: 'numeric' , second: 'numeric' , hour12: true }) %> <br>
                                Date: <%= new Date(blog.created_date).toLocaleDateString('en-US', { weekday: 'short' ,
                                    year: 'numeric' , month: 'short' , day: 'numeric' }) %>
                        </p>
                    </div>

                    <div class="update_date">
                        <h4 style="color: #444; font-size: 16px; margin-bottom: 5px;">Updated At</h4>
                        <% if (blog.updated_date) { %>
                            <p style="color: #555; font-size: 14px;">
                                Time: <%= new Date(blog.updated_date).toLocaleString('en-US', { hour: 'numeric' ,
                                    minute: 'numeric' , second: 'numeric' , hour12: true }) %> <br>
                                    Date: <%= new Date(blog.updated_date).toLocaleDateString('en-US', { weekday: 'short'
                                        , year: 'numeric' , month: 'short' , day: 'numeric' }) %>
                            </p>
                            <% } else { %>
                                <h5 style="color: #777; font-size: 14px;">No Updates Yet</h5>
                                <% } %>
                    </div>

                </div>
                <div class="nav">
                    <a aria-label="Update Blog" href="/admin/<%=blog._id%>/update"><button
                            style="background-color: green;">Update</button></a>
                    <button class="delete-button" type="button"
                        onclick="showDeleteCard('<%= blog._id %>')">Delete</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Delete confirmation card -->
    <div id="deleteCard">
        <p>Are you sure you want to delete this blog?</p>
        <button id="cancelButton">Cancel</button>
        <button id="confirmDeleteButton">Delete</button>
    </div>

    <!-- Overlay -->
    <div id="overlay" onclick="hideDeleteCard()"></div>
</body>
<script src="/JavaScript/adminBlock.js"></script>

</html>